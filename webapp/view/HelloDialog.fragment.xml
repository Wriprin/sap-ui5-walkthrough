<core:FragmentDefinition
   xmlns="sap.m"
   xmlns:core="sap.ui.core" >
   <Dialog
      id="helloDialog"
      title="Hello {/recipient/name}">

      <!--
         1. 添加一个 关闭 Dialog 的 OK Button
         - 这里我们使用了 Dialog 的一个 Aggregation - beginButton
         - 它保证了 内部 button 为 ltr（从左侧渲染）
         - 这样做的意义是，按照常理 确定 在 左侧，否定 在 右侧
         - 这样一来，如果再加一个 否定按钮，那就应该是 endButton（rtl（从右侧渲染）） - Button
      -->
      <beginButton>
         <Button
            text="{i18n>dialogCloseButtonText}"
            press=".onCloseDialog"
            type="Accept"/>
      </beginButton>

   </Dialog>
</core:FragmentDefinition>