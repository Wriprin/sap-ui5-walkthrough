<mvc:View
   xmlns="sap.m"
   controllerName="sap.ui.demo.walkthrough.controller.InvoiceList"
   xmlns:mvc="sap.ui.core.mvc">

   <List
      id="invoiceList"
      class="sapUiResponsiveMargin"
      width="auto"
      items="{
         path : 'invoice>/Invoices',
         sorter : [
            {
               path : 'ShipperName',
               group : true
            },
            {
               path : 'ExtendedPrice',
               descending : true
            }
         ]
      }" >

      <!-- 搜索框 -->
      <headerToolbar>
         <Toolbar>
            <Title text="{i18n>invoiceListTitle}"/>
            <ToolbarSpacer />
            <SearchField width="35%" search=".onFilterInvoices"/>
         </Toolbar>
      </headerToolbar>

      <!-- 行项数据 -->
      <items>
         <ObjectListItem
            title="{invoice>Quantity} x {invoice>ProductName}"
            number="{
               parts: [{path: 'invoice>ExtendedPrice'}, {path: 'view>/currency'}],
               type: 'sap.ui.model.type.Currency',
               formatOptions: {
                  showMeasure: false
               }
            }"
            numberUnit="{view>/currency}"
            numberState="{= ${invoice>ExtendedPrice} > 50 ? 'Error' : 'Success'}"
            type="Navigation"
            press="onPress">
            <!-- 5. 添加一个 Press 事件，并设置 Type 为 Nav，在对应 Controller 中撰写 Nav 路由导航逻辑，跳转到 Detail 页面 -->

               <firstStatus>
                  <ObjectStatus 
                     text="{
                        path: 'invoice>Status',
                        formatter: '.formatter.statusText'
                     }"/>
               </firstStatus>

            </ObjectListItem>
      </items>
   </List>

</mvc:View>