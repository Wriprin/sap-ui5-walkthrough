<mvc:View
   xmlns="sap.m"
   controllerName="sap.ui.demo.walkthrough.controller.InvoiceList"
   xmlns:mvc="sap.ui.core.mvc">

   <!-- 
      - Step 24: Sorting and Grouping
      1. 对 items 添加 sorter，指定 path 和 排序对象（sorter-path）
      2. 默认升序排列，如果想要降序排列，添加「descending : true」即可
      3. 想要分组，添加「group : true」即可
      4. 实现次排序：转为 数组，再加一条即可
    -->
   <List
      id="invoiceList"
      class="sapUiResponsiveMargin"
      width="auto"
      items="{
         path : 'invoice>/Invoices',
         sorter : [
            {
               path : 'ShipperName',
               group : true
            },
            {
               path : 'ExtendedPrice',
               descending : true
            }
         ]
      }" >

      <!-- 搜索框 -->
      <headerToolbar>
         <Toolbar>
            <Title text="{i18n>invoiceListTitle}"/>
            <ToolbarSpacer />
            <SearchField width="35%" search=".onFilterInvoices"/>
         </Toolbar>
      </headerToolbar>

      <!-- 行项数据 -->
      <items>
         <ObjectListItem
            title="{invoice>Quantity} x {invoice>ProductName}"
            number="{
               parts: [{path: 'invoice>ExtendedPrice'}, {path: 'view>/currency'}],
               type: 'sap.ui.model.type.Currency',
               formatOptions: {
                  showMeasure: false
               }
            }"
            numberUnit="{view>/currency}"
            numberState="{= ${invoice>ExtendedPrice} > 50 ? 'Error' : 'Success'}">

               <firstStatus>
                  <ObjectStatus 
                     text="{
                        path: 'invoice>Status',
                        formatter: '.formatter.statusText'
                     }"/>
               </firstStatus>

            </ObjectListItem>
      </items>
   </List>

</mvc:View>